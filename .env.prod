# Defaults de producción — SIN secretos, por eso este archivo sí se commitea.
# Los valores sensibles van en .env.prod.local (nunca se commitea, ver .env.prod.local.example).

APP_ENV=prod
APP_DEBUG=0
