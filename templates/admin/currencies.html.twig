{% extends 'admin_layout.html.twig' %}

{% block title %}Monedas - Saldo Banca{% endblock %}

{% block body %}
<div id="vue-app"
    data-list-url="{{ path('admin_currency_list') }}"
    data-create-url="{{ path('admin_currency_create') }}"
    data-detail-url="{{ path('admin_currency_update', {id: '__ID__'}) }}"
    data-status-url="{{ path('admin_currency_status', {id: '__ID__'}) }}"
></div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
{{ vite_entry_script_tags('currencies') }}
{% endblock %}
