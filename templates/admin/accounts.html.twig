{% extends 'admin_layout.html.twig' %}

{% block title %}Clientes - SaldoGrin{% endblock %}

{% block body %}
<div id="vue-app"
    data-list-url="{{ path('admin_account_index') }}"
    data-create-url="{{ path('admin_account_create') }}"
    data-detail-url="{{ path('admin_account_update', {id: '__ID__'}) }}"
    data-status-url="{{ path('admin_account_status', {id: '__ID__'}) }}"
    data-currencies-list-url="{{ path('admin_currency_list') }}"
    data-system-currencies-url="{{ path('admin_account_system_currencies') }}"
    data-payout-accounts-url="{{ path('admin_business_payout_account_index', {accountId: '__ACCOUNT_ID__'}) }}"
></div>
{% endblock %}

{% block javascripts %}
{{ parent() }}
{{ vite_entry_script_tags('accounts') }}
{% endblock %}
